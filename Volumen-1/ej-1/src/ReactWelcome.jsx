import React from 'react';

const ReactWelcome = () => {
    const styles = {
        color: 'blue',
        fontSize: '2rem',
        textAlign: 'center',
        marginTop: '20px'
    };

    return (
        <h1 style={styles}>¡Bienvenido a mi aplicación en React!</h1>
    );
};

export default ReactWelcome;
