import React from 'react';
import './StyledBox.css'; // Importamos el archivo CSS externo

const StyledBox = () => {
    return (
        <div className="styled-box">
            Este es un div con estilos aplicados.
        </div>
    );
};

export default StyledBox;
